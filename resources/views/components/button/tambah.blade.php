@props(['link'])

<a class="justify-self-end flex mx-3" href="{{ $link }}">
    <button type="button"
        class="justify-self-end items-center py-2 px-7 bg-zinc-800 rounded-md font-semibold flex hover:scale-110 hover:cursor-pointer">
        <p class="text-zinc-50">+Tambah</p>
    </button>
</a>
